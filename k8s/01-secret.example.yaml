# TEMPLATE — não contém segredos reais e não deve ser aplicado direto.
# O Secret real é gerado pelo scripts/deploy.sh com openssl rand, uma única vez,
# e depois preservado entre deploys. Este arquivo existe só para documentar as chaves.
apiVersion: v1
kind: Secret
metadata:
  name: finance-secrets
  namespace: finance
type: Opaque
stringData:
  DB_PASSWORD: "troque-me"
  JWT_SECRET: "troque-me-por-64-bytes-aleatorios"
